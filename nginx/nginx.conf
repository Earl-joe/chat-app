events {
    http {
        server {
            listen 80;

            location / {
                proxy_pass http://frontend:80;
                proxy_set_heady X-Real-IP $remote_addr;


            }
        }
    }
}